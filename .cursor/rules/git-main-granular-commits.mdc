---
description: After every meaningful change set, granular English commits and push origin main — do not wait for the user to ask
alwaysApply: true
---

# Git: granular commits → push main (always)

This project rule **overrides** any habit of waiting for “please commit”. After a meaningful change set that you finished in this chat, ship it.

## Required after meaningful work

1. Split the work into the **largest practical number of small commits** (one logical concern each).
2. Write detailed **English** commit messages (why + what).
3. Commit on **`main` only**.
4. **`git push origin main`** after the commit batch — without waiting for a second “push” message.
5. **Never** create a feature branch, never open a PR, never push elsewhere — unless the user explicitly overrides in that message.

## What counts as “meaningful”

- Any finished feature, fix, UI cleanup, engine change, docs update, or Cursor rule that leaves the tree dirty when you would otherwise end the turn.
- Do **not** leave finished work uncommitted because a generic “only commit when asked” rule exists elsewhere — **this file wins for this repo**.

## Deploy after push

- If Hostinger (or other) **Git auto-deploy** is enabled for this repo/`main`, the push **is** the deploy — say so and check deployment status.
- If auto-deploy is off and the user asked to see it live (or the session is shipping product changes), deploy to the known Hostinger site after push, confirming overwrite only when the tool is a full replace and the domain is ambiguous.

## How to split

Prefer separate commits for:

- Cursor rules / agent config
- Design tokens / theme / Badge / Soft CSS
- Deleting or replacing legacy UI
- Engine / server / API wiring
- Shared `lib/` contracts, i18n, nav, seed data
- App shell (layout, providers, sidebar, header)
- One page or view at a time when changes are large
- Docs / README

Avoid one mega-commit that mixes unrelated areas.

## Examples

```text
✅ good
fix(ui): Soft Badge secondary uses muted chips, not Button SA-700
feat(shell): collapse sidebar with offcanvas instead of icon rail
docs: record Soft Badge vs Button secondary rule

❌ bad
feat: Soft foundation, UEP port, badge fix, sidebar, docs, and engines
```

## Safety

- Do not force-push `main` unless the user explicitly asks.
- Do not commit secrets (`.env`, credentials, tokens).
- Do not commit huge chat exports or local plan dumps unless the user asked.
- Follow the usual git safety protocol for hooks; fix hook failures with a **new** commit, not amend, unless amend rules are fully met.
